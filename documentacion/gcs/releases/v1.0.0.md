# Checklist de release — v1.0.0

**Estado:** Aprobada (baseline retroactiva)
**Responsable de QA:** Henry Alvarez
**Tag objetivo:** `v1.0.0`
**Commit:** `3c48404` (previo a la integración de biometría)
**Fecha de firma:** 2026-08-29

Esta checklist debe completarse antes de crear el tag. Una casilla pendiente impide declarar la línea base como liberada.

> **Nota sobre el alcance de v1.0.0:** este tag se crea de forma retroactiva sobre el historial existente, previo a establecer el flujo formal de GCS (commit `46b70c0`). Varias casillas de esta sección no pueden marcarse porque el historial anterior no tuvo PR/issue por cambio — se documenta como deuda aceptada, no se oculta. El flujo formal de PR + revisión cruzada + CI obligatoria rige desde `v1.1.0` en adelante.

## Alcance y trazabilidad

- [x] El alcance de la versión y los cambios incluidos están registrados. *(Historial completo hasta `3c48404`; ver `git log`.)*
- [ ] Los issues/RFC, PRs y commits de la versión están enlazados. **No cumplido**: el historial previo a `46b70c0` no usó PR/issue por cambio (commits directos a `main`). Ver problema frecuente detectado en el informe de estado.
- [x] Los EC modificados y su impacto están revisados. *(Mapeados en `CM_PLAN.md`, tabla de elementos de configuración EC-01…EC-09.)*
- [x] `CHANGELOG.md` contiene las notas de versión verificadas.

## Calidad

- [x] CI de backend aprobada: restore y build `Release`. *(GitHub Actions, commit `3c48404`, job "Backend (.NET)": success.)*
- [x] Pruebas .NET existentes ejecutadas y aprobadas, o se documentó formalmente su ausencia. *(No hay proyecto de pruebas .NET versionado; `ci.yml` lo detecta y lo declara explícitamente — ausencia documentada, no silenciada.)*
- [x] CI de frontend aprobada: instalación reproducible, build y Karma en ChromeHeadless. *(GitHub Actions, commit `3c48404`, job "Frontend (Angular)": success.)*
- [ ] Flujos críticos validados: autenticación, inventario, facturación y reparaciones. **No cumplido**: no existe suite de pruebas que cubra estos flujos todavía. Queda como objetivo de QA para próximas versiones.
- [x] No hay secretos, llaves ni archivos locales en el diff de release. *(`.env` está en `.gitignore` de raíz y de `backend/`; solo `backend/.env.example` está versionado y sin valores reales.)*

## Datos, configuración y despliegue

- [ ] Migraciones EF Core y scripts SQL revisados y probados en una base temporal. **No cumplido**: sin evidencia documentada de prueba en base temporal antes de esta versión.
- [x] El orden de ejecución de scripts, incluidos triggers, está documentado. *(`README.MD`, pasos 3–4: migraciones primero, luego `02_triggers.sql`; advierte no ejecutar `01_schema.sql` junto con migraciones.)*
- [x] Las variables de `.env.example` están actualizadas y no contienen valores reales. *(`backend/.env.example` revisado: todos los campos sensibles vacíos.)*
- [ ] Las cargas de `wwwroot/uploads` están clasificadas como fixtures o datos operativos con respaldo definido. **No cumplido**: pendiente según lo previsto en `CM_PLAN.md`.
- [ ] Docker y/o el procedimiento de despliegue fue validado en un entorno limpio. **No cumplido**: sin evidencia de validación en entorno limpio para esta versión.

## Aprobación y publicación

- [ ] PR de release integrado en `main` y árbol de trabajo limpio. **No aplica retroactivamente**: `v1.0.0` es anterior al flujo de PR obligatorio; se acepta como excepción única de la línea base.
- [x] Henry Alvarez aprueba la evidencia de QA, con las excepciones documentadas arriba.
- [x] Se crea el tag anotado `v1.0.0` sobre el commit aprobado (`3c48404`).
- [x] Se publica el tag y un GitHub Release con sus notas.

| Aprobación | Responsable | Fecha | Evidencia/enlace |
| --- | --- | --- | --- |
| Técnica backend | Ramirez Juan | 2026-08-29 | CI backend commit `3c48404`: success |
| Técnica frontend | Jeremy Haro | 2026-08-29 | CI frontend commit `3c48404`: success |
| QA / release | Henry Alvarez | 2026-08-29 | Esta checklist |
