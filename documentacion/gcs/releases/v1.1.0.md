# Checklist de release — v1.1.0

**Estado:** Aprobada con deuda declarada (ver nota de CI)
**Responsable de QA:** Henry Alvarez
**Tag objetivo:** `v1.1.0`
**Commit:** `b6f1728` (merge de PR #1 `feat/biometrica` sobre `3c48404`)
**Fecha:** 2026-08-19 (merge) · Firma: 2026-08-29

## Alcance y trazabilidad

- [x] El alcance de la versión y los cambios incluidos están registrados. *(Acceso biométrico y por PIN en Android, bloqueo de sesión por inactividad, botón físico de retroceso.)*
- [x] Los issues/RFC, PRs y commits de la versión están enlazados. *(PR #1 — `feat/biometrica`, 3 commits: `f68bc74`, `edda84e`, `50dedcd`.)*
- [x] Los EC modificados y su impacto están revisados. *(EC-02 cliente web/móvil, EC-05 dependencias/build — ver diff de PR #1.)*
- [x] `CHANGELOG.md` contiene las notas de versión verificadas.

## Calidad

- [x] CI de backend aprobada: restore y build `Release`. *(Commit `50dedcd`: success.)*
- [x] Pruebas .NET existentes ejecutadas y aprobadas, o se documentó formalmente su ausencia. *(Sin proyecto de pruebas .NET; ausencia documentada en `ci.yml`.)*
- [ ] CI de frontend aprobada: instalación reproducible, build y Karma en ChromeHeadless. **NO cumplido**: el job "Frontend (Angular)" falló en `50dedcd` y en el merge `b6f1728` — `app.spec.ts` no compila el árbol de inyección de `App` (`BackButtonService → AuthService → HttpClient`, error `NG0201`). Reproducido localmente el 2026-08-29 con el mismo resultado exacto que en Actions.
- [ ] Flujos críticos validados: autenticación, inventario, facturación y reparaciones. **No cumplido**: sigue sin suite de pruebas de estos flujos.
- [x] No hay secretos, llaves ni archivos locales en el diff de release. *(Diff de PR #1 revisado: sin `.env` ni credenciales.)*

## Datos, configuración y despliegue

- [ ] Migraciones EF Core y scripts SQL revisados y probados en una base temporal. **No aplica**: PR #1 no modifica EC-03 (no toca `Migrations` ni `DatabaseScripts`).
- [x] El orden de ejecución de scripts, incluidos triggers, está documentado. *(Sin cambios respecto a `v1.0.0`.)*
- [x] Las variables de `.env.example` están actualizadas y no contienen valores reales. *(Sin cambios respecto a `v1.0.0`.)*
- [ ] Las cargas de `wwwroot/uploads` están clasificadas como fixtures o datos operativos con respaldo definido. **No cumplido** (deuda heredada de `v1.0.0`, sin cambios en esta versión).
- [ ] Docker y/o el procedimiento de despliegue fue validado en un entorno limpio. **No cumplido** (deuda heredada de `v1.0.0`).

## Aprobación y publicación

- [x] PR de release integrado en `main` y árbol de trabajo limpio. *(PR #1, mergeado como `b6f1728`.)*
- [x] Henry Alvarez aprueba la evidencia de QA **con la excepción explícita de la CI de frontend**, que queda registrada como el problema frecuente detectado en esta práctica.
- [x] Se crea el tag anotado `v1.1.0` sobre el commit aprobado (`b6f1728`).
- [x] Se publica el tag y un GitHub Release con sus notas.

## Acción de seguimiento

La CI de frontend en rojo se corrige en **Issue #3 → PR #4** (`fix/3-app-spec-httpclient`) y se libera como **`v1.1.1`** (parche, sin nueva funcionalidad). Ver `documentacion/gcs/releases/v1.1.1.md`.

| Aprobación | Responsable | Fecha | Evidencia/enlace |
| --- | --- | --- | --- |
| Técnica backend | Ramirez Juan | 2026-08-19 | CI backend commit `50dedcd`: success |
| Técnica frontend | Jeremy Haro | 2026-08-29 | CI frontend commit `50dedcd`: **failure** (excepción documentada arriba) |
| QA / release | Henry Alvarez | 2026-08-29 | Esta checklist |
